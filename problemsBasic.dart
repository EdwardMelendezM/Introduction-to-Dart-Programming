//Do these:
// 1. Test the methods of var -> string





void main(){
  var cadena = "    hola a todos gAAAA";
  var vacio = "";
  // Null nulo = null;
  // double numero = 100.88;
  print("This is the ejecercices");
  print(cadena.split(" "));
  print(cadena.substring(0,9));
  print(cadena.trim());
  print(cadena.toUpperCase());
  print(cadena.toLowerCase());
  print(cadena.toString());
  if(vacio.isEmpty){
    print("Is vacio");
  }
  // print("-----------");
  // print(numero.isFinite);
  // print(numero.isNaN);
  // print(numero.isNegative);
  // print(numero.toInt());
  // print(numero.toDouble());

}